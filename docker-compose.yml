version: '3.8'

services:
  flask_app:
    volumes:
      - ./Scores.txt:/app/Scores.txt

    container_name: wog_flask
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8777:8777"


    restart: unless-stopped
